import React, { useState, useEffect } from 'react';
import { AsyncStorage } from 'react-native';

/*export default class teste{

  constructor(value){
    
      if (value === 'true') {
        await AsyncStorage.setItem('usuario', "teste 2");
        await AsyncStorage.setItem('senha', "teste 2");
        let ret = {
          usuario:'teste 1',
          senha:'teste 1'
        }
        AsyncStorage.getItem('usuario').then(v => {
          ret.usuario = v;
        });
        AsyncStorage.getItem('senha').then(v => {
          ret.senha = v;
        });
        return ret;
      }
  }

}*/
// default teste(value);